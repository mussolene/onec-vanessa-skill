# Cursor Adapter

Read [../../core/SKILL.md](../../core/SKILL.md) first.

Rules:

- reuse existing repo conventions before generating new ones;
- keep UI and xUnit flows separate;
- favor small, reviewable edits and the minimum rerun scope;
- store failure context in `artifacts/` and point back to it in follow-up work;
- never claim runtime support that is not explicitly documented and verified.

